<% content_for :body_class do %>detail organization<% end %>

<% content_for :title do %>
  Results for “<%= @org.name %>”
<% end %>

<div id="column_fullWidth">

  <div class="org_metaDetails">
    <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Morbi risus risus, elementum a congue id, euismod et felis. Nam sed dolor et velit viverra pulvinar ac nec massa. Integer nec sodales tellus. Etiam hendrerit accumsan lacus eget ultrices. Donec consequat felis eget sapien faucibus semper pharetra tortor rutrum.<%= @org.description %></p>

    <div id="dataDetails">
      <div id="metaDetails">
        <dl>
          <%= detail_field @org, :attribute => :org_type, :label => "Type",
          :value => @org.org_type, :titleize => true, :noclear => true %>

          <%= detail_field @org, :attribute => :level, :label => "Level",
          :value => @org.level, :titleize => true, :noclear => true  %>

          <%= detail_field @org, :attribute => :acronym, :label => "Acronym",
          :value => "ABC", :noclear => true  %>

          <%= detail_field @org, :attribute => :names, :collection => true, :label => "Other Names",
          :value => @org.names, :noclear => true  %>

          <%= detail_field @org, :attribute => :url, :label => "URL",
          :value => truncate(@org.url, :length => 35), :url => @org.url, :noclear => true  %>

        </dl>        
      </div><!-- metaDetails -->

      <div class="clear"></div> 

    </div><!-- dataDetails -->

    <%= render 'shared/pagination', :docs => @sources %>
    <div class="clear"></div> 
  </div>  

  <%= show_flash_message %>  
  <%= render 'shared/source_table' %>
  <div class="tableNav">
    <%= render 'shared/pagination', :docs => @sources %>
  </div>
</div>
