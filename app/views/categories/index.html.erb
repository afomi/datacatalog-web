<% content_for :title do %>Categories<% end %>
<br />
<% @categories.each do |category| %>
  <%#= link_to(category.name, :controller => :browse, :source_category=> category.name) %>
  <%= link_to(category.name, category_path(category.name.downcase)) %>
  <br />
<% end %>
